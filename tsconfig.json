{
  "compilerOptions": {
    "target": "es2016",                          // ✅ Good
    "module": "commonjs",                        // ✅ Node-compatible
    "rootDir": "./",                             // 👈 Needed so TypeScript knows where source files start
    "outDir": "./dist",                          // 👈 Needed so compiled JS goes into /dist folder
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "esModuleInterop": true,                     // 👈 Needed for importing modules like 'express'
    "skipLibCheck": true
  },
  "exclude": ["node_modules", "dist"]
}
